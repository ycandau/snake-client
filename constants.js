const IP = 'localhost'
const PORT = 50541
const INTERVAL = 50
const INITIALS = 'YC'

const DIRECTIONS = {
  w: 'up',
  a: 'left',
  s: 'down',
  d: 'right',
}

const MESSAGES = {
  O: 'Ouroboros',
  '?': 'Who am I?',
  '~': 'I am sinuous!',
  '>': 'Catch me if you can.',
}

module.exports = { IP, PORT, INTERVAL, INITIALS, DIRECTIONS, MESSAGES }
